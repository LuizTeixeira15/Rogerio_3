<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rogerio_3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_nassau_3"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_nassau_3"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".restau"></activity>
        <activity android:name=".hotel" />
        <activity android:name=".turismo" />
        <activity android:name=".restaurante" />
        <activity android:name=".bar" />
        <activity android:name=".Bares" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>